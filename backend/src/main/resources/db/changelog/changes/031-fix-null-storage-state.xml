<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                   http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.24.xsd">

    <!-- Fix: seed 013 tạo admin + genres không set storage_state → NULL → bị lọc ra -->
    <changeSet id="031-fix-null-storage-state" author="cinex">
        <sql>
            UPDATE users SET storage_state = 'ACTIVE' WHERE storage_state IS NULL;
            UPDATE genres SET storage_state = 'ACTIVE' WHERE storage_state IS NULL;
            UPDATE movies SET storage_state = 'ACTIVE' WHERE storage_state IS NULL;
            UPDATE rooms SET storage_state = 'ACTIVE' WHERE storage_state IS NULL;
            UPDATE seats SET storage_state = 'ACTIVE' WHERE storage_state IS NULL;
            UPDATE showtimes SET storage_state = 'ACTIVE' WHERE storage_state IS NULL;
            UPDATE bookings SET storage_state = 'ACTIVE' WHERE storage_state IS NULL;
            UPDATE snacks SET storage_state = 'ACTIVE' WHERE storage_state IS NULL;
            UPDATE vouchers SET storage_state = 'ACTIVE' WHERE storage_state IS NULL;
            UPDATE reviews SET storage_state = 'ACTIVE' WHERE storage_state IS NULL;
        </sql>
    </changeSet>

</databaseChangeLog>
